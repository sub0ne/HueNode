class Group {

    // properties
    _lights = [];
    _type = undefined;
    _on = false;

    constructor(parameters) {
        super(parameters);
    }

    createGroup(name, type, lightIDs) {        
    }

}

module.exports = Group;